**日期**: 2026-03-18
**今日目标**: 基于离线 PBF + Open-Meteo 构建 `nyc_2017_pristine_v9.csv`，并在新数据上跑历史模型 v7/v6 与 v8(LMM) 对照。

**核心操作与代码变动**:
- 新增并运行 `scripts/check_offline_consistency.py`（Phase 0 离线一致性检查）。
- 更新 `scripts/check_offline_consistency.py`:
  - 修复 OSM edge centroid 的 CRS 处理（先投影再求中点）。
  - 增加 v9 输出的 v8 兼容列对齐逻辑（补 `WEATHER_CONDITION`, `REAL_WEATHER`，对齐列顺序）。
  - 支持 `--force_overwrite` 输出 `nyc_2017_pristine_v9.csv`。
- 修复 `scripts/sample_two_stage_v7.py` 的 CUDA 张量转 numpy 问题（`num_classes_expanded`/`num_classes` CPU 化）。
- 在 `nyc_2017_pristine_v9.csv` 上完成:
  - v7: `prepare_data_v7.py` -> `train_causal_v7.py` -> `sample_two_stage_v7.py` -> `evaluate_v7.py`
  - v6: `prepare_data_v3.py` -> `train_causal_v6.py` -> `postprocess_synthetic_v3.py --skip_api` -> `evaluate_v3.py`
  - v8 ablation: free/hard/soft(gemini low-quota) + `v8_ablation_eval.py`

**遇到的 Bug 与解决方案**:
- [Bug] `sample_two_stage_v7.py` 在 GPU 上报错: `TypeError: can't convert cuda:0 device type tensor to numpy`。
- [Solution] 对 `diff.num_classes` 和 `diff.num_classes_expanded` 在转 numpy 前执行 `detach().cpu()`。
- [Bug] Gemini API 持续 429 (`RESOURCE_EXHAUSTED`) 导致 soft 阶段耗时过长。
- [Solution] 使用低配额配置 `quota_profile=low` + `gemini_max_rows=5`，保留 LLM 路径尝试并完成产物。

**实验结果与感悟**:
- Phase 0 一致性报告: `exp/phase0/offline_consistency_report_v9.json`
  - `overall_discrepancy_rate = 0.4772`（高于 0.35）
  - 通过 `--force_overwrite` 生成 `nyc_2017_pristine_v9.csv` 供后续实验使用。
- v7(v9) 评估: `exp/nyc_crash_v7/causal_m4_v7_v9/eval_v7_v9.json`
  - `logic_violation_rate = 0.0`
  - `numeric_drift_avg = 0.4701`, `categorical_tv_avg = 0.0644`
- v6(v9) 评估: `exp/nyc_crash_v3/causal_m4_v6_v9/evaluation_report.json`
  - `avg_js_divergence = 0.0168`
  - `task2_primary_cause test_macro_f1 = 0.743`
  - `task3_is_injury test_auc = 0.7036`
- v8(v9, LLM路径) 评估: `exp/nyc_crash_v8_ablation_v9_llm/v8_ablation_metrics.json`
  - soft 模式在当前配额下主要回退到规则惩罚；Gemini 仅尝试前 5 行。

**明日计划**:
- 优先收敛 Phase 0 高差异特征（`TEMP_C`, `WIND_SPEED_KMH`, `coco`, `DIST_TO_SIGNAL_M`, `OSM_SPEED_TAG`）的映射口径。
- 在配额恢复后重跑 v8 soft(gemini) 以获得真实 LLM 惩罚增益。
- 统一 v6/v7/v8 的核心可比指标，生成跨方法对照表（因果逻辑一致性 + 任务效用 + 分布保真）。
