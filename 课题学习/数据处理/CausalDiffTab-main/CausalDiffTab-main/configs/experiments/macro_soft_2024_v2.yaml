# CausalDiffTab experiment config — v2 schema (45-col, 2024 data)
# 完全体 (Ours)：宏观软因果骨架 + 分层 Inpainting (Stage1+Stage3)
# lambda=0.3 软正则，适配 2024 数据分布
model_name: macro_soft_2024_v2
experiment_id: macro_soft_2024_v2
lambda_causal: 0.3
use_causal_masks: true
hierarchical: true
mask_subdir: causal_masks_soft
description: "完全体 (Ours)：宏观软因果骨架 + 分层 Inpainting，lambda=0.3，2024 45-col 45 schema"
sampling:
  mode: impute_stage3
  num_samples: 10000
  impute_indices_count: 10000
