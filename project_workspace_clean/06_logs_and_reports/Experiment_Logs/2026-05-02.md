# 2026-05-02 实验日志

## 2024 源域正式迁移评价线

### 目标口径

- 训练源域：`data/nyc_crash_2024/train.csv`。
- 域内测试：`data/nyc_crash_2024/test.csv`。
- 迁移测试：`data/nyc_crash_2025/test.csv`。
- 合成输出目录：`results/synthetic_2024_source`。
- 评价 schema 固定为：`data/nyc_crash_2024/info.json`，避免误用旧 `data/nyc_crash/info.json`。

### 已完成的改进

- 正式迁移评价从旧的 2017-source 漂移诊断切换为 2024-source：训练用 2024 train，分别评估 2024 test 与 2025 test。
- `src/prepare_dataset.py` 支持 `--input_test_csv/--full_dataname/--stage1_dataname`，可保留既有 2024 train/test split 并生成 TabDiff 所需的 `X_num_*.npy/X_cat_*.npy/y_*.npy` 与 causal masks。
- `src/train_hierarchical.py` 支持 `--dataname nyc_crash_2024`，Stage 1 会自动映射到 `nyc_stage1_2024`。
- `pipeline/run_all_experiments.py`、`pipeline/run_external_baselines.py`、`pipeline/evaluate_all.py`、`pipeline/run_finish_pipeline.py` 已支持 source dataname、独立 synthetic dir 与 source info json。
- `pipeline/rebuild_2017_p1.py` 静态类型问题已修复：`extract_osm_lanes()` 返回类型改为 `tuple[pd.DataFrame, pd.DataFrame]`，并显式处理 `pyrosm.OSM.get_network()` 返回 `None` 的分支。
- `pipeline/run_finish_pipeline.py` 汇总表中的完整模型名从旧 `macro_soft_2024` 修正为正式 `ours_full_model`。
- 发现并修复 `pipeline/run_all_experiments.py` 的 Stage 1 dataname 漏传问题：完整模型启动时 Stage 1 一度误用默认 `nyc_stage1`（2017-source，`train=173533`），已主动中止该次运行；现在 Stage 1/3 都显式接收 `--dataname nyc_crash_2024`，由 `train_hierarchical.py` 映射到 `nyc_stage1_2024`（2024-source，`train=66952`）。

### 实验配置

| 模型 | 因果掩码 | 分层 | 采样方式 | 状态 |
| --- | --- | --- | --- | --- |
| `baseline_tabddpm` | 否 | 否 | unconditional | 已完成 |
| `ablation_no_causal` | 否 | 是 | impute_stage3 | 已完成 |
| `ablation_no_hierarchy` | 是 | 否 | unconditional | 已完成 |
| `ours_full_model` | 是 | 是 | impute_stage3 | 已完成 |

### 已完成训练与采样

| 模型 | 训练指标 | 产物 | 说明 |
| --- | ---: | --- | --- |
| `baseline_tabddpm` | Stage 3 `best_loss=1.8997` | `results/synthetic_2024_source/baseline_tabddpm_full.csv` | early stopping at epoch 979；训练、采样、后处理完成 |
| `ablation_no_causal` | Stage 1 `best_loss=2.7232`；Stage 3 `best_loss=1.9057` | `results/synthetic_2024_source/ablation_no_causal_full.csv` | early stopping at Stage 1 epoch 1288、Stage 3 epoch 740；训练、采样、后处理完成 |
| `ablation_no_hierarchy` | Stage 3 `best_loss=1.8826` | `results/synthetic_2024_source/ablation_no_hierarchy_full.csv` | early stopping at epoch 1192；训练、采样、后处理完成 |
| `ours_full_model` | Stage 1 `best_loss=3.2313`；Stage 3 `best_loss=1.8594` | `results/synthetic_2024_source/ours_full_model_full.csv` | early stopping at Stage 1 epoch 1263、Stage 3 epoch 1532；训练、采样、后处理完成 |

PowerShell/Tee/tqdm 组合在完成后仍可能返回 `exit code 1`；本轮以日志中 `[done] 合成数据` 与最终 CSV/meta 文件落盘作为成功判据。

### 正式评价指标

最终统一评估由 `pipeline/run_finish_pipeline.py --tier full` 触发，内部调用 `pipeline/evaluate_all.py`，分两套测试集输出报告：

- 2024 域内：`results/eval_report_full_source2024_in_domain.*`。
- 2025 迁移：`results/eval_report_full_source2024_transfer2025.*`。
- 综合汇总：`results/eval_full_source2024_compare.md`。

核心指标口径：

- `mean_wasserstein_numeric`：连续变量边际分布距离，越低越好。
- `mean_js_categorical`：分类变量 Jensen-Shannon 距离，越低越好。
- `tstr_avg_score`：TSTR 下游任务平均分，用合成训练、真实测试，越高越好。
- `tstr_best_model` / `tstr_r2` / `tstr_mse` / `tstr_mae`：TSTR 最优下游模型及回归质量指标。
- `primary_metrics_profile=no_rule`：正式主指标暂不把规则违背率作为主排序项，聚焦分布保真与迁移 TSTR。

### 最终评估结果

`pipeline/run_finish_pipeline.py --tier full` 已完成。PowerShell 由于 warning 流仍返回 `exit code 1`，但外部 baseline、域内评估、迁移评估和综合报告均已写出：

- 外部 baseline：`baseline_ctgan_full.csv`、`baseline_tvae_full.csv`、`baseline_smote_full.csv`。
- 2024 域内报告：`results/eval_report_full_source2024_in_domain.md/json`。
- 2025 迁移报告：`results/eval_report_full_source2024_transfer2025.md/json`。
- 综合汇总：`results/eval_full_source2024_compare.md`。

#### 2024 域内测试

| 模型 | W-num ↓ | JS-cat ↓ | TSTR avg ↑ | best model | TSTR R2 ↑ | MSE ↓ | MAE ↓ |
| --- | ---: | ---: | ---: | --- | ---: | ---: | ---: |
| `ablation_no_causal` | 0.312912 | 0.012825 | 0.728240 | random_forest | 0.607075 | 0.289388 | 0.197792 |
| `ablation_no_hierarchy` | 0.435871 | 0.008997 | 0.721118 | random_forest | 0.624083 | 0.276862 | 0.221527 |
| `baseline_tabddpm` | 0.274842 | 0.009202 | 0.725090 | random_forest | 0.611522 | 0.286113 | 0.225740 |
| `ours_full_model` | 0.312912 | 0.012397 | 0.716711 | random_forest | 0.617501 | 0.281710 | 0.202435 |
| `baseline_smote` | 5.382007 | 0.172975 | 0.653081 | random_forest | 0.453657 | 0.402381 | 0.215070 |
| `baseline_tvae` | 225.015299 | 0.106413 | 0.439943 | random_forest | 0.308900 | 0.508994 | 0.544325 |
| `baseline_ctgan` | 204.175004 | 0.096816 | 0.334692 | random_forest | -0.366938 | 1.006747 | 0.822156 |

域内结论：2024 test 上四个 TabDiff/CausalDiffTab 系模型整体接近。`ablation_no_causal` 的 `tstr_avg_score` 最高，`ablation_no_hierarchy` 的单模型 `tstr_r2` 最高，`baseline_tabddpm` 的数值边际距离最低；`ours_full_model` 在域内不是第一，但仍明显优于 CTGAN/TVAE/SMOTE 外部 baseline。

#### 2025 迁移测试

| 模型 | W-num ↓ | JS-cat ↓ | TSTR avg ↑ | best model | TSTR R2 ↑ | MSE ↓ | MAE ↓ |
| --- | ---: | ---: | ---: | --- | ---: | ---: | ---: |
| `ours_full_model` | 0.843438 | 0.014135 | 0.732988 | random_forest | 0.611184 | 0.280323 | 0.203800 |
| `ablation_no_hierarchy` | 1.013877 | 0.012866 | 0.724803 | random_forest | 0.609996 | 0.281180 | 0.225614 |
| `baseline_tabddpm` | 0.845373 | 0.013974 | 0.636342 | random_forest | 0.608972 | 0.281918 | 0.226705 |
| `ablation_no_causal` | 0.843438 | 0.015213 | 0.572526 | random_forest | 0.605940 | 0.284104 | 0.196907 |
| `baseline_smote` | 5.745623 | 0.173563 | 0.625753 | random_forest | 0.431261 | 0.410041 | 0.217381 |
| `baseline_tvae` | 224.829288 | 0.107719 | 0.462765 | random_forest | 0.301764 | 0.503405 | 0.532796 |
| `baseline_ctgan` | 203.980602 | 0.095192 | 0.311872 | xgboost | -0.325674 | 0.955766 | 0.788581 |

迁移结论：2025 transfer 上 `ours_full_model` 综合排名第 1，`tstr_avg_score=0.732988`、`tstr_r2=0.611184`，略高于无分层消融并明显高于 `baseline_tabddpm`。这说明完整的“因果掩码 + 分层生成”组合主要收益体现在跨年迁移稳定性，而不是单纯 2024 域内拟合。

#### 排名摘要

- 2024 域内综合排名：`ablation_no_causal` > `ablation_no_hierarchy` > `baseline_tabddpm` > `ours_full_model` > `baseline_smote` > `baseline_tvae` > `baseline_ctgan`。
- 2025 迁移综合排名：`ours_full_model` > `ablation_no_hierarchy` > `baseline_tabddpm` > `ablation_no_causal` > `baseline_smote` > `baseline_tvae` > `baseline_ctgan`。
- 外部 baseline 中 `SMOTE` 明显强于 CTGAN/TVAE，但仍低于 TabDiff 系模型；CTGAN/TVAE 在数值 Wasserstein 和 TSTR 上均出现明显退化。

### 最终导出表还原修正

发现 `results/synthetic_2024_source/ours_full_model_full.csv` 虽然来自 `nyc_crash_2024` 训练源域，但它仍是评估/训练空间 CSV：包含 `is_*` 车辆/原因二值列、`CRASH_TIME_SIN/COS` 等中间特征。此前后处理补 `CRASH DATE` 时默认引用了 2017 pristine 表，因此查看 CSV 时会出现 2017 日期；这不是训练源域错回 2017，而是最终导出引用表未按 2024 source 切换。

已新增 `pipeline/export_raw_style_synthetic.py`，专门把训练空间合成表导出为“原始 NYC crash 表头 + API 补全列”样式：

- 日期从 2024 原始事故表日期池按 `SEASON/DAY_OF_WEEK` 重采样，最终 `CRASH DATE` 年份检查为 `[2024]`。
- `is_sedan/is_suv/.../is_other_vehicle` 还原为 `VEHICLE TYPE CODE 1-5`。
- `is_distracted/is_speeding/...` 还原为 `CONTRIBUTING FACTOR VEHICLE 1-5`。
- `NUMBER_*_BIN` 还原为原始伤亡计数字段。
- 旧版 `REAL_SPEED_LIMIT` 在已生成的 2024 训练表中退化为单一中间尺度常数 `-0.128317...`，raw-style 导出时只能反变换为 `25.0 mph` 用于可读展示；这不是足够真实的路段限速分布。
- 通过 2024 原始事故表最近邻补 `BOROUGH/ZIP CODE/ON STREET NAME/CROSS STREET NAME/OFF STREET NAME`。
- 最终导出不包含 `is_*`、`CRASH_TIME_SIN/COS`、`SEASON`、`DAY_OF_WEEK`、`TIME_PERIOD` 等训练中间列。

当前可交付文件：`results/synthetic_2024_source/ours_full_model_full_raw_style.csv`，共 10000 行、47 列。`ours_full_model_full.csv` 继续保留为评估用训练空间表，避免破坏已生成的评估报告。

### Stage 1/2/3 结构与域内消融诊断

当前分层结构不是三个都训练扩散模型，而是“两段模型 + 一段确定性上下文”：

| Stage | 角色 | 变量 | 当前实现 |
| --- | --- | --- | --- |
| Stage 1 | 时空锚点 | `LATITUDE/LONGITUDE/CRASH_TIME_SIN/CRASH_TIME_COS` + `SEASON/DAY_OF_WEEK/TIME_PERIOD` | `train_hierarchical.py --stage 1` 训练 `nyc_stage1_2024` |
| Stage 2 | 离线 API/OSM/天气上下文 | `TEMP_C/prcp/WIND_SPEED_KMH/DIST_TO_SIGNAL_M/REAL_SPEED_LIMIT/INFERRED_LANES` + `HAS_TRAFFIC_SIGNAL/OSM_ONEWAY/HAS_DIVIDER/coco/WEATHER_CONDITION/OSM_TYPE` | 不单独训练；由 `prepare_2025_data.py` 基于坐标/时间查离线天气与 OSM |
| Stage 3 | 事故微观结果 | 车辆类型、原因类型、伤亡 bin、`TOTAL_VEHICLES/IS_MULTI_VEHICLE` 和目标 `NUMBER OF PERSONS INJURED` | `train_hierarchical.py --stage 3` 训练全表扩散模型 |

四组实验实际差异：

- `baseline_tabddpm`：无因果、无分层，Stage 3 全表 unconditional 生成。
- `ablation_no_hierarchy`：有因果掩码、无分层，Stage 3 全表 unconditional 生成。
- `ablation_no_causal`：无因果、有分层，采样时使用 `impute_stage3`。
- `ours_full_model`：有因果、有分层，采样时使用 `impute_stage3`。

域内消融结果不理想的主要原因判断：

1. `impute_stage3` 采样会随机选训练集真实行作为条件，冻结 Stage 1+2，只重采样目标 `y` 和 Stage 3 分类列。也就是说，`ours_full_model` 与 `ablation_no_causal` 的时空、天气、OSM 上下文大部分来自真实训练行，域内 TSTR 容易被真实条件行托高，因果掩码的边际收益被掩盖。
2. `ours_full_model` 和 `ablation_no_causal` 的采样条件相同，差异主要只剩 Stage 3 训练时的因果正则；如果 causal mask 与目标任务不完全对齐，或者正则过强，域内拟合可能略受约束。
3. 当前 2024 训练表中 `REAL_SPEED_LIMIT` 和 `HAS_DIVIDER` 都是常数：`REAL_SPEED_LIMIT=-0.128317...`、`HAS_DIVIDER=0`。这会削弱 Stage 2 路网特征的信息量，也会让因果 mask 中涉及 `REAL_SPEED_LIMIT` 的边没有实际贡献。
4. 2024 域内测试与训练分布接近，简单 unconditional 的 `baseline_tabddpm/ablation_no_hierarchy` 已能拟合得很好；完整模型的优势更容易体现在 2025 迁移测试中，而不是域内测试。
5. 采样评估中 `ours_full_model` 的 `tstr_best_model_score` 仍略高，但 `tstr_avg_score` 被 MLP 等较不稳定模型拉低；因此域内排名不宜只看综合平均，需要同时看 best-model 与迁移结果。

已修正 `prepare_2025_data.py` 的 `REAL_SPEED_LIMIT` 来源：现在优先解析最近 OSM 边的 `maxspeed` 标签并转为 mph；缺失时才按道路等级/NYC 默认限速 fallback（如 residential 25、primary/secondary 30、trunk 40、motorway 50）。OSM 文件使用口径保持不变：2024 训练集使用 `raw_data/osm/2024` 下由 `new-york-250101.osm.pbf` 派生的 graphml；2025 迁移测试使用 `raw_data/osm/2025` 下由 `new-york-260101.osm.pbf` 派生的 graphml。

下一步若要判断域内指标是否优化，需要重建 `nyc_crash_2024/nyc_stage1_2024` 与 `nyc_crash_2025`，再重训四组 full 模型并重新评估；旧报告不能代表修复后 speed-limit 版本。

### 下一轮改进实验提示词：Stage2 可训练化 + NOTEARS 因果图修正

#### 背景判断

当前 2024-source 正式实验已经完成，但完整模型的分层采样仍主要采用 `impute_stage3`：随机选取真实训练行作为条件，冻结 Stage1+Stage2，只重采样目标 `y` 和 Stage3 分类列。这一设置适合“给定真实测试事故上下文的迁移评估”，但不完全适合“未来年份事故数据生成/预测”。

如果目标是生成未来事故数据，则未来没有真实事故行可供参照，`时空、天气、OSM 上下文` 不能长期依赖真实训练行或测试行。Stage2 需要从“离线查表/条件上下文层”升级为“可训练的条件生成层”。

#### 总目标

请基于现有 CausalDiffTab/TabDiff 代码线，设计并实现下一轮改进实验：

```text
Stage1: P(time, space)
Stage2: P(weather, road | time, space)
Stage3: P(vehicle, cause, injury | time, space, weather, road)
```

即：

1. Stage1 生成事故时空锚点。
2. Stage2 在 Stage1 条件下生成天气与 OSM/路网上下文。
3. Stage3 在 Stage1+Stage2 条件下生成车辆类型、事故原因、伤亡类别和受伤人数目标。

最终目标是让模型具备“未来无真实事故行条件”下的完整链式生成能力，而不是只做 Stage3 条件补全。

#### Stage 划分建议

##### Stage1：时空锚点层

保留：`LATITUDE`、`LONGITUDE`、`CRASH_TIME_SIN`、`CRASH_TIME_COS`、`SEASON`、`DAY_OF_WEEK`、`TIME_PERIOD`。

说明：Stage1 是未来生成的初始锚点；可继续训练 `nyc_stage1_2024`，但后续采样不应只从真实训练行取条件，而应支持 Stage1 unconditional 生成。

##### Stage2：环境/路网上下文生成层

新增可训练 Stage2 数据集与模型。

连续变量：`TEMP_C`、`prcp`、`WIND_SPEED_KMH`、`DIST_TO_SIGNAL_M`、`REAL_SPEED_LIMIT`、`INFERRED_LANES`。

分类变量：`HAS_TRAFFIC_SIGNAL`、`OSM_ONEWAY`、`HAS_DIVIDER`、`coco`、`WEATHER_CONDITION`、`OSM_TYPE`。

训练形式：

```text
Stage2 target = weather + road context
Stage2 condition = Stage1 time + space
```

实现要求：

1. 新增 `nyc_stage2_2024` 数据目录。
2. 新增 Stage2 causal masks。
3. `train_hierarchical.py` 支持 `--stage 2`。
4. `sample_conditional.py` 支持 Stage1→Stage2 条件采样。
5. 对未来生成任务，Stage2 不再直接引用真实训练行的天气/OSM 上下文。

##### Stage3：事故微观结果层

Stage3 继续生成车辆类型二值列、事故原因二值列、伤亡 bin、`TOTAL_VEHICLES`、`IS_MULTI_VEHICLE` 和目标 `NUMBER OF PERSONS INJURED`。

采样形式需要从当前：

```text
真实训练行 Stage1+2 -> impute Stage3
```

改进为：

```text
生成 Stage1 -> 生成 Stage2 -> 条件生成 Stage3
```

#### 因果图修正规则

NOTEARS 算法发现的图只能作为候选统计结构，不能直接作为最终因果图。最终图必须经过领域规则修正。特别注意：事故时间影响路网属性的边不合理，应强制删除。

##### 强制删除边

- `CRASH_TIME_SIN/CRASH_TIME_COS/SEASON/DAY_OF_WEEK/TIME_PERIOD -> OSM 路网属性`
- `天气变量 -> 静态 OSM 路网属性`
- `伤亡结果 -> 时间/空间/天气/路网`
- `事故原因/伤亡结果 -> 车辆类型`
- `事故结果变量 -> Stage1/Stage2 上下文变量`

其中 OSM 路网属性包括：`DIST_TO_SIGNAL_M`、`REAL_SPEED_LIMIT`、`INFERRED_LANES`、`HAS_TRAFFIC_SIGNAL`、`OSM_ONEWAY`、`HAS_DIVIDER`、`OSM_TYPE`。

原因：路网属性来自 OSM 快照和经纬度空间匹配；事故发生时间不会导致限速、车道数、道路类型、单行属性变化；2024 与 2025 使用不同 OSM 快照，这是年份/场景层面的外生设置，不应被建模成单条事故样本中的时间→路网因果边。

##### 强制保留边

- `LATITUDE/LONGITUDE -> OSM 路网属性`
- `时间/季节 -> 天气`
- `天气/路网 -> 事故原因`
- `时间 -> 事故原因`
- `车辆类型 -> 事故原因`
- `车辆类型 -> TOTAL_VEHICLES/IS_MULTI_VEHICLE`
- `事故原因 -> 伤亡结果`
- `车辆类型 -> 伤亡结果`
- `天气/路网 -> 伤亡结果`，建议弱约束

##### 可选场景节点

如果后续显式加入年份或路网快照变量，可使用 `YEAR / OSM_SNAPSHOT -> OSM 路网属性`，但不要用事故时间变量代替年份/快照变量。

#### 宏观因果结构

下一版仍应沿用“宏观骨架 + soft mask”的思路，不回退到旧版 222 边密集图。

推荐宏观结构：

```text
时间/空间锚点 -> 天气/路网环境 -> 事故原因/行为 -> 伤亡结果
车辆类型 -> 事故原因/行为 -> 伤亡结果
空间坐标 -> OSM 路网属性
```

可采用三档权重：强保留边 `1.0`，弱约束边 `0.5`，删除边 `0.0`。

#### 数据修正前置要求

1. 重新运行 `prepare_2025_data.py`，确保 `REAL_SPEED_LIMIT` 优先来自 OSM `maxspeed`，缺失时才 fallback。
2. 检查 2024 train 中 `REAL_SPEED_LIMIT` 不应再是单一常数。
3. 如果 `HAS_DIVIDER` 仍为常数，需要判断 OSM 中是否确实缺失 divider 信息，或考虑删除/弱化该字段。
4. 重新构建 `data/nyc_crash_2024`、`data/nyc_stage1_2024`、新增 `data/nyc_stage2_2024` 与 `data/nyc_crash_2025`。
5. 重新生成 causal masks，确保修正后的黑白名单已生效。

#### 实验组设计

| 实验组 | Stage2 | 因果图 | 采样方式 |
| --- | --- | --- | --- |
| `baseline_tabddpm` | 不单独训练 | 无 | full-table unconditional |
| `ablation_no_causal` | 可训练 | 无因果 mask | Stage1→Stage2→Stage3 |
| `ablation_no_stage2` | 查表/真实条件 | 有因果 mask | Stage1+真实 Stage2→Stage3 |
| `ablation_no_hierarchy` | 无分层 | 有因果 mask | full-table unconditional |
| `ours_stage2_causal` | 可训练 | 修正后 soft causal mask | Stage1→Stage2→Stage3 |

重点比较 Stage2 可训练化是否提升未来生成闭环能力、修正后因果图是否减少不合理边带来的过约束、完整链式生成在 2024 域内和 2025 迁移上的表现，以及 Stage2 生成质量是否可信。

#### 评价指标

沿用原有 `mean_wasserstein_numeric`、`mean_js_categorical`、`tstr_avg_score`、`tstr_r2`、`tstr_mse`、`tstr_mae`。

新增 Stage2 专项评估：天气变量分布距离、路网变量分布距离、OSM 分类 JS、`LATITUDE/LONGITUDE -> DIST_TO_SIGNAL_M/OSM_TYPE` 条件一致性、`SEASON/TIME_PERIOD -> TEMP_C/prcp` 条件一致性、负降水/非法限速/非法车道数/未知 OSM 类型等规则违背率，以及 Stage1→Stage2→Stage3 链式生成完整性。

#### 产物要求

请输出并记录：修改后的 Stage1/2/3 字段划分、修正后的因果黑白名单、新的 causal matrix/mask 文件、Stage2 数据目录与 `info.json`、Stage2 训练 checkpoint、链式采样脚本或参数说明、2024 域内评估报告、2025 迁移评估报告、raw-style 最终导出表，以及为什么 Stage2 从查表层改为生成层、为什么删除事故时间→路网属性边、为什么保留空间→路网属性边、NOTEARS 发现图与领域修正图区别的说明。

### Stage2 可训练化落地与数据真实性修正

本轮已按上面的改进提示词完成第一阶段代码落地：

- `src/prepare_dataset.py` 新增 `--stage2_dataname`，现在一次准备会生成 `nyc_stage1_2024`、`nyc_stage2_2024` 与 `nyc_crash_2024` 三套数据目录。
- `nyc_stage2_2024` 字段为 Stage1 时空锚点 + Stage2 天气/OSM 上下文，训练规模 `train=66952`、`test=16739`，mask 维度为 `num=(11, 11)`、`cat=(63, 63)`。
- `src/train_hierarchical.py` 支持 `--stage 2`，Stage2 checkpoint 目录规则为 `ckpt/nyc_stage2_2024/stage2_context_<tier>_<experiment_id>`。
- `src/sample_conditional.py` 支持 `--impute_stage stage2|stage3`：`stage2` 会冻结 Stage1 并重采样天气/OSM 上下文；`stage3` 继续冻结 Stage1+2 并重采样事故结果。
- `pipeline/run_all_experiments.py` 支持新模型名 `ours_stage2_causal`、`train_stage2: true` 与 `sampling.mode=impute_stage2/impute_stage3`。
- 新增 `configs/experiments/ours_stage2_causal.yaml`，用于 Stage1 + Stage2 + Stage3 的层级训练配置。

因果图方面，`prepare_dataset.py` 在 NOTEARS/宏观候选矩阵之上加入领域规则：强制删除事故时间/天气到静态 OSM 路网属性、Stage3 结果到 Stage1/2 上下文、原因/伤亡到车辆类型等不合理边；强制保留或提升空间到路网、时间到天气、天气/路网到事故原因、车辆到原因/伤亡、原因到伤亡等领域合理边。本轮正式 mask 生成日志为 `removed=2, enforced_or_upweighted=507`，Stage3 full mask 为 `num=(11, 11), edges=28`、`cat=(150, 150), edges=5674`。

数据真实性方面，发现正式 `data/nyc_crash_2024/train.csv` 一度仍被旧训练空间常数覆盖，`REAL_SPEED_LIMIT=-0.128317...`，而 `test.csv` 和小样本 speedcheck 已是正确物理 mph。已对 `pipeline/prepare_2025_data.py` 做两点加固：

1. `_LEVEL_B_OSM` 纳入 `REAL_SPEED_LIMIT` 与 `HAS_DIVIDER`，避免这些 OSM 物理字段漏入旧 `train_ref` median/mode fallback。
2. 新增写出前后 `REAL_SPEED_LIMIT` 分布打印，正式重建时校验 `train`、`written train`、`written test` 是否一致。

带校验全量重建输出：

```text
[OSM] REAL_SPEED_LIMIT 分布: nunique=11, min=5.0, max=55.0
[OSM] train REAL_SPEED_LIMIT: nunique=11, min=5.0, max=55.0
[OSM] written train REAL_SPEED_LIMIT: nunique=11, min=5.0, max=55.0
[OSM] written test REAL_SPEED_LIMIT: nunique=11, min=5.0, max=55.0
```

验证通过后已同步为正式 `data/nyc_crash_2024`。当前正式训练集限速分布：`REAL_SPEED_LIMIT nunique=11`，top 为 `{25.0: 46608, 30.0: 10704, 50.0: 5118, 20.0: 2432, 45.0: 867, 40.0: 741, 35.0: 268, 15.0: 103, 55.0: 55, 10.0: 40}`。这说明 Stage2 训练不再使用旧归一化常数限速。

`HAS_DIVIDER` 仍为单一类别，当前判断是 2024 GraphML 中 `divider/median/separation/barrier` 等标签覆盖不足导致。后续实验不应把 `HAS_DIVIDER` 当成有效区分信号；若正式 full 实验中仍无贡献，可考虑从 Stage2 字段中删除或在因果 mask 中弱化。

补充说明：本轮 OSM 数据重建没有使用 GPU，因为 `prepare_2025_data.py` 的瓶颈是 `osmnx/geopandas/shapely/sklearn BallTree/pandas` 的 GraphML 加载、投影、最近道路匹配和表处理，基本是 CPU/内存密集型流程；GPU 主要用于后续 `train_hierarchical.py --device cuda:0` 的扩散模型训练与采样。

当前状态仍不是最终“未来闭环生成”完成版：已具备 Stage2 可训练数据、训练入口和 Stage2 inpainting 采样；真正的 `生成 Stage1 -> 编码到 Stage2 张量空间 -> 生成 Stage2 -> 编码到 Stage3 张量空间 -> 生成 Stage3` 闭环脚本仍需继续实现。

GPU quick smoke 已完成：

```text
python src/train_hierarchical.py --stage 2 --tier quick --device cuda:0 --no_wandb --lambda_causal 0.1 --experiment_id ours_stage2_causal_gpu_smoke --dataname nyc_crash_2024
```

结果：Stage2 使用 `cuda:0`，`best_loss=4.0428`，checkpoint 写入 `ckpt/nyc_stage2_2024/stage2_context_quick_ours_stage2_causal_gpu_smoke`。随后执行：

```text
python src/sample_conditional.py --ckpt_dir ckpt/nyc_stage2_2024/stage2_context_quick_ours_stage2_causal_gpu_smoke --data_dir data/nyc_stage2_2024 --condition_train_indices "0,1,2,3,4" --impute_stage stage2 --num_samples 20 --batch_size 10 --device cuda:0 --output_csv results/_stage2_2024_gpu_smoke_context_samples.csv --no_postprocess
```

采样成功写出 20 行 Stage2 context samples。

### `ours_stage2_causal balanced` 全链训练与正式评估

在修正后的 2024 正式数据上，已继续执行 `ours_stage2_causal` 的 balanced 档全链实验：

```text
python pipeline/run_all_experiments.py --model ours_stage2_causal --tier balanced --device cuda:0 --dataname nyc_crash_2024 --synthetic_dir results/synthetic_2024_stage2_causal_balanced
```

训练与采样已完成。PowerShell 终端最终仍返回 `exit code 1`，但该轮和前面正式实验一样，是 warning/管道返回码问题；本轮以 `[done] 合成数据` 和最终 CSV 落盘作为成功判据。

本轮 balanced 训练结果：

- Stage 1 `best_loss=3.2782`
- Stage 2 `best_loss=2.7698`
- Stage 3 `best_loss=2.3371`

最终合成产物：

- `results/synthetic_2024_stage2_causal_balanced/ours_stage2_causal_balanced.csv`
- `results/synthetic_2024_stage2_causal_balanced/ours_stage2_causal_balanced_raw_style.csv`
- `results/synthetic_2024_stage2_causal_balanced/_ours_stage2_causal_balanced_samples_physical.csv`
- `results/synthetic_2024_stage2_causal_balanced/_ours_stage2_causal_balanced_sampling_meta.json`

其中 physical summary 显示：`REAL_SPEED_LIMIT mean=28.043, std=7.306, [5.001, 54.999]`，说明本轮 balanced 合成已经使用修复后的物理限速分布，而不是旧的单一归一化常数。`HAS_DIVIDER` 仍然只有单类 `0.0=2000`，这一点和训练数据现状一致，暂时仍不应视为有效区分特征。

随后已对该 balanced 合成表执行正式外部评估：

```text
python pipeline/evaluate_all.py --real_test data/nyc_crash_2024/test.csv --synthetic_dir results/synthetic_2024_stage2_causal_balanced --file_glob "ours_stage2_causal_balanced.csv" --task_type regression --primary_metrics_profile no_rule --info_json data/nyc_crash_2024/info.json --output_tag ours_stage2_causal_balanced_in_domain

python pipeline/evaluate_all.py --real_test data/nyc_crash_2025/test.csv --synthetic_dir results/synthetic_2024_stage2_causal_balanced --file_glob "ours_stage2_causal_balanced.csv" --task_type regression --primary_metrics_profile no_rule --info_json data/nyc_crash_2024/info.json --output_tag ours_stage2_causal_balanced_transfer
```

生成报告：

- `results/eval_report_ours_stage2_causal_balanced_in_domain.md/json`
- `results/eval_report_ours_stage2_causal_balanced_transfer.md/json`

#### 2024 域内结果

| 模型 | W-num ↓ | JS-cat ↓ | TSTR avg ↑ | best model | TSTR R2 ↑ | MSE ↓ | MAE ↓ |
| --- | ---: | ---: | ---: | --- | ---: | ---: | ---: |
| `ours_stage2_causal_balanced` | 0.324123 | 0.012809 | 0.555409 | random_forest | 0.339948 | 0.486127 | 0.395953 |

与前面 full 档正式 2024-source 结果相比：

- 低于 `baseline_tabddpm`：`W-num=0.274842`，`TSTR avg=0.725090`
- 低于 `ablation_no_causal`：`TSTR avg=0.728240`
- 低于 `ablation_no_hierarchy`：`TSTR avg=0.721118`
- 也低于旧的 `ours_full_model`：`W-num=0.312912`，`JS-cat=0.012397`，`TSTR avg=0.716711`

结论：截至当前这版 `ours_stage2_causal balanced`，Stage2 可训练化 + 修正因果图并没有把 2024 域内结果拉高，反而明显下降；尤其是 TSTR 从原来约 `0.72` 降到 `0.56` 左右，说明当前实现下 balanced 档的闭环收益没有在域内兑现。

#### 2025 迁移结果

| 模型 | W-num ↓ | JS-cat ↓ | TSTR avg ↑ | best model | TSTR R2 ↑ | MSE ↓ | MAE ↓ |
| --- | ---: | ---: | ---: | --- | ---: | ---: | ---: |
| `ours_stage2_causal_balanced` | 3.660520 | 0.016532 | 0.512691 | random_forest | 0.312177 | 0.495897 | 0.407982 |

迁移结果同样弱于此前 full 档 `ours_full_model` 的正式 2025 指标：旧结果为 `W-num=0.843438`、`JS-cat=0.014135`、`TSTR avg=0.732988`、`TSTR R2=0.611184`。因此当前 balanced 版不仅没有带来更强迁移，反而出现了明显退化。

#### 当前判断

这轮实验能回答用户关心的问题：在当前实现里，“Stage2 可训练化 + NOTEARS 修正后的因果 mask” 还没有直接把效果变好，域内和迁移都没有优于已完成的 full 档正式结果。

更可能的原因是：

1. 当前 `ours_stage2_causal` 虽然新增了 Stage2 训练，但最终采样配置仍不是完整 `Stage1 -> Stage2 -> Stage3` 闭环未来生成脚本，而是现有层级采样流程上的过渡方案。
2. balanced 档训练规模和收敛程度明显低于此前 full 档正式实验，当前结果不能证明“Stage2 思路无效”，只能说明“这版 balanced 实现还没有把收益跑出来”。
3. `HAS_DIVIDER` 仍为常数、Stage2 路网字段有效信息仍有限，也会削弱新增 Stage2 的潜在收益。

如果后续要继续判断这个方向是否可行，下一步应优先做两件事：

- 实现真正的闭环 `Stage1 -> Stage2 -> Stage3` 链式采样，而不是继续依赖当前过渡采样方式。
- 在 full 档而不是 balanced 档上，重新跑 `ours_stage2_causal`，再与现有 full 档 `ours_full_model` 做同口径对比。

### `ours_stage2_causal full` 闭环链式采样与正式对比

已按上面的两项后续动作继续落地：

1. 在 `src/sample_conditional.py` 中新增真正的 `Stage1 -> Stage2 -> Stage3` 闭环链式采样入口，不再依赖 `condition_train_indices` 从真实训练行抽条件。
2. 在 `pipeline/run_all_experiments.py` 中新增 `sampling.mode=chain_stage123` 分支，自动按顺序调用 `stage1_spatial_*`、`stage2_context_*`、`stage3_full_*` 三个 checkpoint 完成链式生成。
3. `configs/experiments/ours_stage2_causal.yaml` 已切换为 `sampling.mode: chain_stage123`，因此 `ours_stage2_causal` 的正式运行口径已从旧过渡方案改为真正闭环。

核心实现细节：

- Stage1 先做 unconditional 生成，得到时空锚点。
- Stage2 不再使用真实训练行做条件，而是把 Stage1 生成结果重新编码到 `nyc_stage2_2024` 的张量空间，冻结 Stage1 列，放开 Stage2 上下文列和 Stage2 数据目录中的目标列后执行 sample-impute。
- Stage3 再把 Stage1+Stage2 生成结果编码到 `nyc_crash_2024` 的张量空间，冻结 Stage1+2，放开 Stage3 结果列与目标列执行 sample-impute。
- 链式 smoke 已通过，产物包括中间上下文文件：`_chain_stage123_balanced_smoke_stage1_chain.csv` 与 `_chain_stage123_balanced_smoke_stage2_chain.csv`。

随后已执行 full 档正式实验：

```text
python pipeline/run_all_experiments.py --model ours_stage2_causal --tier full --device cuda:0 --dataname nyc_crash_2024 --synthetic_dir results/synthetic_2024_stage2_causal_full
```

该轮 full 日志关键完成标记：

- Stage 1 `best_loss=3.2311`
- Stage 2 `best_loss=2.4353`
- Stage 3 `best_loss=1.8972`
- 已进入 `Hierarchical CausalDiffTab - Chain Sampler`
- 最终 `[done] 合成数据: results/synthetic_2024_stage2_causal_full/ours_stage2_causal_full.csv`

正式合成产物：

- `results/synthetic_2024_stage2_causal_full/ours_stage2_causal_full.csv`
- `results/synthetic_2024_stage2_causal_full/ours_stage2_causal_full_raw_style.csv`
- `results/synthetic_2024_stage2_causal_full/_ours_stage2_causal_full_samples_stage1_chain.csv`
- `results/synthetic_2024_stage2_causal_full/_ours_stage2_causal_full_samples_stage2_chain.csv`
- `results/synthetic_2024_stage2_causal_full/_ours_stage2_causal_full_sampling_meta.json`

physical summary 显示：`REAL_SPEED_LIMIT mean=27.472, std=6.456, [15.000, 50.000]`；Stage2 路网上下文已来自模型链式生成而不是真实训练行，但 `HAS_DIVIDER` 仍为单类 `0.0=10000`。

随后已做与旧 full 档一致口径的正式评估：

```text
python pipeline/evaluate_all.py --real_test data/nyc_crash_2024/test.csv --synthetic_dir results/synthetic_2024_stage2_causal_full --file_glob "ours_stage2_causal_full.csv" --task_type regression --primary_metrics_profile no_rule --info_json data/nyc_crash_2024/info.json --output_tag ours_stage2_causal_full_chain_in_domain

python pipeline/evaluate_all.py --real_test data/nyc_crash_2025/test.csv --synthetic_dir results/synthetic_2024_stage2_causal_full --file_glob "ours_stage2_causal_full.csv" --task_type regression --primary_metrics_profile no_rule --info_json data/nyc_crash_2024/info.json --output_tag ours_stage2_causal_full_chain_transfer
```

生成报告：

- `results/eval_report_ours_stage2_causal_full_chain_in_domain.md/json`
- `results/eval_report_ours_stage2_causal_full_chain_transfer.md/json`

#### 2024 域内结果：闭环 full 对比旧 full

| 模型 | W-num ↓ | JS-cat ↓ | TSTR avg ↑ | best model | TSTR R2 ↑ | MSE ↓ | MAE ↓ |
| --- | ---: | ---: | ---: | --- | ---: | ---: | ---: |
| `ours_stage2_causal_full` | 1.255655 | 0.011058 | 0.661463 | random_forest | 0.610969 | 0.286521 | 0.213368 |
| `ours_full_model` | 0.312912 | 0.012397 | 0.716711 | random_forest | 0.617501 | 0.281710 | 0.202435 |

对比结论：

- 新闭环版的分类边际距离略好：`JS-cat 0.011058 < 0.012397`
- 但数值边际距离明显变差：`W-num 1.255655 > 0.312912`
- 域内任务分数仍低于旧 full：`TSTR avg 0.661463 < 0.716711`
- `TSTR R2` 与旧 full 接近，但仍略低：`0.610969 < 0.617501`

因此，在 2024 域内上，真正闭环 full 版相较 balanced 版确实明显回升，但仍没有超过现有正式 `ours_full_model`。

#### 2025 迁移结果：闭环 full 对比旧 full

| 模型 | W-num ↓ | JS-cat ↓ | TSTR avg ↑ | best model | TSTR R2 ↑ | MSE ↓ | MAE ↓ |
| --- | ---: | ---: | ---: | --- | ---: | ---: | ---: |
| `ours_stage2_causal_full` | 4.546750 | 0.012934 | 0.674170 | random_forest | 0.599326 | 0.288872 | 0.231842 |
| `ours_full_model` | 0.843438 | 0.014135 | 0.732988 | random_forest | 0.611184 | 0.280323 | 0.203800 |

对比结论：

- 新闭环版在分类 JS 上略好：`0.012934 < 0.014135`
- 但数值 Wasserstein 明显恶化：`4.546750 > 0.843438`
- 迁移任务分数仍低于旧 full：`TSTR avg 0.674170 < 0.732988`
- `TSTR R2` 也略低：`0.599326 < 0.611184`

因此，在 2025 迁移上，真正闭环 full 版同样没有超过现有正式 `ours_full_model`。

#### 当前结论更新

这两项后续动作现在都已经做完，得到的判断比前面的 balanced 结果更清楚：

1. 真正闭环 `Stage1 -> Stage2 -> Stage3` 已经实现并能稳定生成 full 样本。
2. 闭环 full 版相较之前的 balanced 过渡实现，质量显著提高，说明问题不只是“Stage2 思路本身错误”，而是过渡采样方式和训练档位确实会严重影响结果。
3. 但即便换成真正闭环 full 版，当前 `ours_stage2_causal` 仍没有超过既有正式 `ours_full_model`；它只在 `JS-cat` 上略有改善，主要短板仍是数值边际分布，尤其是跨年迁移时的 `mean_wasserstein_numeric` 明显偏大。

当前更合理的后续优化方向应从“继续证明能不能闭环”转向“为什么闭环后数值层漂移变大”：重点检查 Stage1 生成的时空分布是否把 Stage2 路网/天气拉到了偏离真实支持域的位置，以及 Stage2 数值列在链式条件下的误差是否被 Stage3 继续放大。
