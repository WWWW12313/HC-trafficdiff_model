# CausalDiffTab experiment config
# 完全体：宏观软因果骨架 (7组约束, 226边) + 分层 Inpainting (Stage1+Stage3)
# 对应 2024→2025 迁移学习场景，lambda_causal=0.3 (弱正则, 容忍分布漂移)
model_name: macro_soft_2024
experiment_id: macro_soft_2024
lambda_causal: 0.3
use_causal_masks: true
hierarchical: true
description: 宏观软因果骨架 + 分层生成 (Stage1+Stage3)，lambda=0.3，2024训练/2025迁移
sampling:
  mode: unconditional
  num_samples: 10000
